# Plan cache: the orchestrator caches the Planner's emit list and skips the LLM while the ready-set is unchanged — migrated

> **Migrated to the Teahouse repo** ([TeahouseHQ/teahouse](https://github.com/TeahouseHQ/teahouse)) per ADR-0014.
> This ADR records an engine decision; it now lives at `docs/adr/0010-plan-cache-skips-planner-while-ready-set-unchanged.md` in that repo. Stub kept so existing links do not dead-end.
